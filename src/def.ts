import * as applet    from '@nodeswork/applet';

export interface FifaFut18AccountInfo {
  credits:           number;
  listingSize:       number;
  listedItems:       number;
  clubValue:         number;
  transferListSize:  number;
}
